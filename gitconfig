[credential]
	helper = osxkeychain

[user]
	name = Nick Burns
	email = nburns@users.noreply.github.com

[core]
	editor = vim
	excludesfile = ~/.gitignore_global

[push]
	default = current

[diff]
    algorithm = patience
    tool = vimdiff

[filter "lfs"]
	required = true
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
